<%= render 'current_user' %>

<%= render partial: 'event', locals: { event: @event } %>
<%= render partial: 'attendances/form',
           locals: { event: @event, attendance: @attendance } %>

<%= render partial: 'attendances/attendances',
           locals: { attendances: @event.attendances } %>
